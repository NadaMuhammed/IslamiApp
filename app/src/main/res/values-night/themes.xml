<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IslamiApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/night_blue</item>
        <item name="colorPrimary">@color/night_blue</item>
        <item name="colorSecondary">@color/gold</item>
        <item name="background">@color/night_blue</item>
    </style>
</resources>